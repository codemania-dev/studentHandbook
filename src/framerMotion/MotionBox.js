/**
 * You can add as many custom components as you want to your project.
 * The likes of ul, li, and a are all valid components in Chakra UI.
 */

import { Box } from '@chakra-ui/react';
import { motion } from 'framer-motion';

const MotionBox = motion(Box);

export default MotionBox;
